{
  "title": "Basic waitgroup func context usage",
  "targets": [
    "spawner"
  ],
  "level": "spawner",
  "variants": {
    "good": {
      "description": "Basic - func uses ctx (waitgroup)",
      "functions": {
        "spawner": "goodBasicWaitGroup"
      }
    },
    "bad": {
      "description": "Basic - func doesn't use ctx (waitgroup)",
      "functions": {
        "spawner": "badBasicWaitGroup"
      }
    }
  }
}
